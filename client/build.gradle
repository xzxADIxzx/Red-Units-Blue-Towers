apply plugin: 'java'

sourceSets.main.java.srcDirs = ['src']

ext {
    arcVersion = 'v139'
}

dependencies {
    compileOnly "com.github.Anuken.Arc:arc-core:$arcVersion"
    compileOnly "com.github.Anuken.Arc:arcnet:$arcVersion"

    compileOnly project(':core')
}